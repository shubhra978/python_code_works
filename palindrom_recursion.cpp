#include <bits/stdc++.h>
using namespace std;

bool palindrome(str my_string, int index){
    
    if(my_string.length() ==0){
        return true;
    }
    else if(my_string[0]!=my_string[index]{
        return false;
    }
    else{
        palindrome(str my_string, int index)
    }
    
}


int main() {
	str one = "hello";
	int index = one.length();
	palindrome(one,index-1)
	
	
}
